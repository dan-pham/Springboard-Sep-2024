import { toUpperCase, toLowerCase, capitalize, add, subtract, multiply } from "./utils/index.mjs";

const song = "THIS is Halloween";
console.log(toUpperCase(song));
console.log(toLowerCase(song));
console.log(capitalize(song));

const first = 10;
const second = 5;
console.log(add(first, second));
console.log(subtract(first, second));
console.log(multiply(first, second));
